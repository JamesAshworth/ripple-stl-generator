# ripple-stl-generator
Tiny TypeScript app to generate a box with a rippled surface.

90% of the code generated by Claude AI, final tweaks by human hands.

To run, clone the repository, run `npm install` then `npm run dev`.
